import { styled } from "@mui/material";

export const useStyles = styled((theme) => ({
  toolbar: {
    display: "flex",
    flexFlow: "row wrap",
    justifyContent: "space-between",
  },

  logo: {
    color: "white",
  },
}));
